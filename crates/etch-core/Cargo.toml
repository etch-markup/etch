[package]
name = "etch-core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"]}
serde_json = "1.0.149"
serde_yaml = "0.9.33"

[dev-dependencies]
insta = { version = "1.46.3", features = ["json"]}